import React from 'react';
import { useParams } from 'react-router-dom';

const UpdateVehicle = () => {
  const { id } = useParams();

  return (
    <div>
      <h1>dddd</h1>
    </div>
  );
};

export default UpdateVehicle;
